# Server Configuration
PORT=3001

# CORS - comma-separated list of allowed origins
# Example: https://your-frontend.railway.app,http://localhost:5173
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Database path (Railway provides ephemeral storage, so use /tmp for development)
# For persistence on Railway, you'd need to use a proper database service
DATABASE_PATH=./data/fundraise.db
